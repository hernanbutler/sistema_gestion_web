import { NgModule } from '@angular/core';
import { SharedModule } from '@shared/shared.module';

const MODULES: any = [SharedModule];

@NgModule({
  declarations: [],
  imports: [MODULES],
})
export class HomeModule {}
